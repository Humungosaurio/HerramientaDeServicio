IMPORTANTE
para ejecutar el programa con el backend activo deben:

2 en una terminal, abran el proyecto normalmente con pnpm
3 en otra terminal (no cierren la primera) ingresen a la carpeta backend con "cd BackEnd" y luego ejecuten el main con:
python main.py